apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: goldbox-rpg-data
  namespace: goldbox-rpg
  labels:
    app: goldbox-rpg
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Optional: specify storage class
  # storageClassName: standard
