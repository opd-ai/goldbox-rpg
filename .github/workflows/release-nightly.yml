# Automated nightly builds published as a pre-release
name: Release (Nightly)

on:
  push:
  schedule:
    # Run daily at midnight UTC
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  # Cross-compile server binary for all supported platforms
  build:
    name: Build (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            suffix: ''
          - goos: windows
            goarch: amd64
            suffix: '.exe'
          - goos: darwin
            goarch: amd64
            suffix: ''
          - goos: darwin
            goarch: arm64
            suffix: ''
          - goos: android
            goarch: arm64
            suffix: ''
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Build binary
        env:
          CGO_ENABLED: '0'
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          go build -trimpath -ldflags="-s -w" \
            -o goldbox-rpg-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.suffix }} \
            ./cmd/server

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: goldbox-rpg-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.suffix }}
          path: goldbox-rpg-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.suffix }}

  # Capture showcase screenshots via headless browser playtest
  screenshots:
    name: Capture Screenshots
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Download dependencies
        run: go mod download

      - name: Generate placeholder assets
        run: make assets-placeholders

      - name: Run browser playtest (captures screenshots)
        run: go test ./test/browser/... -v -timeout 5m -run TestBrowserPlaytest
        continue-on-error: true

      - name: Upload screenshots artifact
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: test/browser/screenshots/screenshot-*.png
          if-no-files-found: warn

  # Collect all artifacts and publish/update the nightly pre-release
  release:
    name: Update Nightly Release
    needs: [build, screenshots]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
      - name: Install NDK and build Go service
        run: |
          sdkmanager "ndk;26.1.10909125"
          cd cmd/android-service && bash build.sh
      - name: Build debug APK
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: '8.5'
          arguments: assembleDebug
          build-root-directory: cmd/android-service/android
      - name: Copy APK to artifacts
        run: cp cmd/android-service/android/app/build/outputs/apk/debug/*.apk artifacts/

      # Remove previous nightly release and tag to allow a fresh upload
      - name: Delete existing nightly release
        run: gh release delete nightly --yes --cleanup-tag || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create nightly pre-release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: nightly
          name: Nightly Build
          body: |
            Automated nightly build from `${{ github.ref_name }}` branch.
            **Commit:** ${{ github.sha }}

            ## Screenshots

            | Splash Screen | Main Menu |
            |:---:|:---:|
            | ![Splash Screen](https://github.com/opd-ai/goldbox-rpg/releases/download/nightly/screenshot-splash-screen.png) | ![Main Menu](https://github.com/opd-ai/goldbox-rpg/releases/download/nightly/screenshot-main-menu.png) |

            | Character Creation | Gameplay |
            |:---:|:---:|
            | ![Character Creation](https://github.com/opd-ai/goldbox-rpg/releases/download/nightly/screenshot-character-creation.png) | ![Gameplay](https://github.com/opd-ai/goldbox-rpg/releases/download/nightly/screenshot-gameplay.png) |
          prerelease: true
          files: artifacts/*
